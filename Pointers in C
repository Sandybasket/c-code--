#include <stdio.h>

void update(int *a,int *b) {
    int s=*a;
    int d=*b;
    *a=s+d;
    *b=abs(s-d);
        
}

int main() {
    int a, b,s,d;
    int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    update(pa, pb);
    printf("%d\n%d", a, b);

    return 0;
}
